import 'package:flutter/material.dart';

class SpendingsScreen extends StatelessWidget {
  const SpendingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("SpendingScreen");
  }
}
